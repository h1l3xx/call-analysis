{
    log {
        output stdout
        level debug
    }
}

{$DOMAIN:localhost} {
    route /grafana* {
        reverse_proxy grafana:3000 {
            header_up X-Forwarded-Prefix /grafana
        }
    }

    reverse_proxy frontend:80
}
